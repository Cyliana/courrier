<form method="post" action="{{ path('destinataire_delete', {'id': destinataire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destinataire.id) }}">
    <button class="btn">Supprimer</button>
</form>
